{
  "name": "functions",
  "main": "lib/index.js",
  "engines": {
    "node": "18"
  },
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "echo \"Skipping lint\"",
    "deploy": "firebase deploy --only functions:sendContactMail"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.1",
    "googleapis": "^133.0.0"
  }
}